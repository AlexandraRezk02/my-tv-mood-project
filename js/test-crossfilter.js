$('div.buttons').tagSort({
	items:'.dvdContainer',
	tagElement: 'button',
	sortType: 'exclusive',
});

/*$().ready(function() {
    $('div.buttons').each(function() {
        $(this).tagSort({
            items: '.dvdContainer',
            tagElement: 'button',
			sortType: 'exclusive',
        });
    });
});*/